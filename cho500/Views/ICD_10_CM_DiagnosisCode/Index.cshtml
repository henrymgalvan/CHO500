@model IEnumerable<cho500.Entity.ICD_10_CM_DiagnosisCode>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.ICD_10_CM_CodeRange.CodeRange)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.DiagnosisCodeRange)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        @*<td>
            @Html.DisplayFor(modelItem => item.ICD_10_CM_CodeRange.CodeRange)
        </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.DiagnosisCodeRange)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
